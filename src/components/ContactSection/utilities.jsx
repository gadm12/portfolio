// ContactSection has no logic-oriented helper functions today — it only reads
// portfolioData.contact and a locally-imported asset, both handled directly
// in ContactSection.jsx. This file is kept as a placeholder so the directory
// shape stays consistent with the other components (Footer, Navbar,
// TimelineSection, AboutSection), and so future logic (e.g. contact-form
// validation) has an obvious home.
